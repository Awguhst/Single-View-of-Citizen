# Test-only dependencies. Deliberately kept out of requirements.txt so the
# deployment image does not ship a test runner.
#   pip install -r app/requirements.txt -r app/requirements-dev.txt
pytest>=8.0
httpx>=0.27
